# Copy this file to .env.local (gitignored — via the *.local rule in
# .gitignore — so it never gets committed) and fill in a real key.
# scripts/fetch-covers.mjs reads it automatically.

# Free key from https://www.omdbapi.com/apikey.aspx
OMDB_API_KEY=
